<!doctype html>
<html lang=en">
<head>
{>_head_content/}
</head>
<body>
{>_header/}
<h2>{title} is a <span class="special">{job}</span></h2>
<p id="greeting"></p>
<div class="background"></div>
<small class="caption">(Above image is below 8k, so inlined base64)</small>
<div class="backgroundHi"></div>
<small class="caption">(Above image is above 8k, so optimized but not inlined)</small>
He likes:
<ul>
    {#likes}
        <li>{.}</li>
    {/likes}
</ul>
{>_footer/}
{>_end_content/}
</body>
</html>
